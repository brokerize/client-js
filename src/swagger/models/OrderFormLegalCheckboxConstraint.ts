/* tslint:disable */
/* eslint-disable */
/**
 * brokerize
 * The brokerize API allows clients to implement multi-brokerage with a unified interface. For more information, visit brokerize.com
 *
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from "../runtime";
import {
  Direction,
  DirectionFromJSON,
  DirectionFromJSONTyped,
  DirectionToJSON,
} from "./Direction";
import {
  OrderModel,
  OrderModelFromJSON,
  OrderModelFromJSONTyped,
  OrderModelToJSON,
} from "./OrderModel";

/**
 *
 * @export
 * @interface OrderFormLegalCheckboxConstraint
 */
export interface OrderFormLegalCheckboxConstraint {
  /**
   * If set, only consider this a match if the selected direction is contained in this list.
   * @type {Array<Direction>}
   * @memberof OrderFormLegalCheckboxConstraint
   */
  directions?: Array<Direction>;
  /**
   * If set, only consider this a match if the selected `Exchange.id` is contained in the list.
   * @type {Array<string>}
   * @memberof OrderFormLegalCheckboxConstraint
   */
  exchangeIds?: Array<string>;
  /**
   * If set, only consider this a match if the selected `orderModel` is contained in the list.
   * If a matching constraint includes `'quote'`, the value must also be supplied to
   * `GetQuote` (via `GetQuoteParams.legalCheckboxChecked`), not only at order creation.
   * @type {Array<OrderModel>}
   * @memberof OrderFormLegalCheckboxConstraint
   */
  orderModels?: Array<OrderModel>;
}

export function OrderFormLegalCheckboxConstraintFromJSON(
  json: any,
): OrderFormLegalCheckboxConstraint {
  return OrderFormLegalCheckboxConstraintFromJSONTyped(json, false);
}

export function OrderFormLegalCheckboxConstraintFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): OrderFormLegalCheckboxConstraint {
  if (json === undefined || json === null) {
    return json;
  }
  return {
    directions: !exists(json, "directions")
      ? undefined
      : (json["directions"] as Array<any>).map(DirectionFromJSON),
    exchangeIds: !exists(json, "exchangeIds") ? undefined : json["exchangeIds"],
    orderModels: !exists(json, "orderModels")
      ? undefined
      : (json["orderModels"] as Array<any>).map(OrderModelFromJSON),
  };
}

export function OrderFormLegalCheckboxConstraintToJSONRecursive(
  value?: OrderFormLegalCheckboxConstraint | null,
  ignoreParent = false,
): any {
  if (value === undefined) {
    return undefined;
  }
  if (value === null) {
    return null;
  }

  return {
    directions:
      value.directions === undefined
        ? undefined
        : (value.directions as Array<any>).map(DirectionToJSON),
    exchangeIds: value.exchangeIds,
    orderModels:
      value.orderModels === undefined
        ? undefined
        : (value.orderModels as Array<any>).map(OrderModelToJSON),
  };
}

export function OrderFormLegalCheckboxConstraintToJSON(
  value?: OrderFormLegalCheckboxConstraint | null,
): any {
  return OrderFormLegalCheckboxConstraintToJSONRecursive(value, false);
}
